# Instrukce pro AI agenty

Viz **[`CLAUDE.md`](CLAUDE.md)** — jediný zdroj instrukcí pro tento projekt
(pravidla vč. zákazu git push/EAS bez souhlasu, pipeline, `manual_article.py`
parametry, WP šablona, Android appka, push notifikace).

Rychlý start:
```bash
python main.py        # celá pipeline (RSS → analýza → e-mail report)
python web_app.py     # web UI (Flask :5000)
```
